<tr>
  <td><%= test.title %></td>
  <td><%= test.level %></td>
  <td><%= test.questions.count %></td>
  <td><%= link_to t('navigation.links.show'), admin_test_path(test) %></td>
  <td><%= link_to t('navigation.links.edit'), edit_admin_test_path(test) %></td>
  <td><%= link_to t('navigation.links.delete.method.'), admin_test_path(test), method: :delete, data: { confirm: t('navigation.links.delete.confirm') } %></td>
</tr>
