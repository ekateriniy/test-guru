<h2><%= t('.header') %></h2>

<p>
<%= link_to t('navigation.links.new'), new_admin_test_path %>
</p>

<table>
  <tr>
    <th><%= t('tests.index.body.title') %></th>
    <th><%= t('tests.index.body.level') %></th>
    <th><%= t('tests.index.body.questions') %></th>
    <th colspan="3"><%= t('tests.index.body.action') %></th>
  </tr>
  <%= render @tests %>
</table>
