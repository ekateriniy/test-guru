<tr>
  <td><%= question.body %></td>
  <td><%= link_to t('navigation.links.show'), admin_question_path(question) %></td> 
  <td>| <%= link_to t('navigation.links.edit'), edit_admin_question_path(question) %></td>
  <td>| <%= link_to t('navigation.links.delete.method'), admin_question_path(question), method: :delete, data: { confirm: t('navigation.links.delete.confirm') } %></td>
</tr>
